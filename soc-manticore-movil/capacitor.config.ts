import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'soc-manticore-movil',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
